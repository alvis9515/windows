---
- name: Copiar archivo a máquina remota
  hosts: all
  become: true  # Si es necesario obtener privilegios de superusuario (sudo)

  tasks:
    - name: Copiar archivo desde el host local a la máquina remota
      copy:
        src: /hola.txt  # Ruta del archivo en el host local
        dest: /home/awx/Desktop/
